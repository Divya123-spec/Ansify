import React from "react";
import { FiCheckSquare } from "react-icons/fi";


const TagsSettings = ({post}) => {
  return (

    <div className="container">
     Tags Settings
    </div>
  );
};

export default TagsSettings;
